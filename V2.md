# V2

## Code rule

- all in src/v2 folder
- v1 not surface to the ui, only code remain
- v2 is completedly self contained
-

```
import { WorkbenchV2 } from '@/v2/components/workbench'
const rootRoute = createRootRoute({ component: WorkbenchV2 })
```

## Style UI UX

- mac os styles, exactly similar to imessages app
- translucent native mac os app, apply for primary/secondary/editor title area/bottom panel
- user can drag and drop windows just like native mac app by dragging the primary sidebar, secondary sidebar, editor title area
- we only support macos desktop app for now so make code simple

## Workbench V2

we only code one by one, after done a small step reviewer will review, approve before go next, so do small code at a time

Layout

- primary sidebar: channels, videos, style of mac os app, like imessages
- secondary sidebar: placeholder, empty for now
- status bar: placeholder, empty for now
- bottom panel: placeholder, empty for now
- main area: editor area, we have some editor like: video editor, channel editor, thumbnail editor, all of them is placeholder for now

use battle tested libraries: dockview, resize, etc..
