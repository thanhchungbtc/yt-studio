## The video

Working title: {{ .Title }}
{{- with .Topic }}
Topic: {{ . }}
{{- end }}
Chapters: {{ len .Chapters }}
{{- with .DurationPhrase }}
Running time: {{ . }}
{{- end }}

Chapter structure (in order):
{{ range .Chapters }}
{{ .Ordinal }}. {{ .Title }}
{{- end }}

---

Write the metadata for this video: the title, the one-paragraph description, the
comma-separated keywords, and the thumbnail hook.
{{ if .DurationPhrase }}
A duration-led title uses "{{ .DurationPhrase }}" exactly. A count-led title
leads with {{ len .Chapters }}.
{{- else }}
No running time is available for this video, so do not use a duration-led
title. A count-led title leads with {{ len .Chapters }}.
{{- end }}

The chapter titles above are what the video actually covers — draw the niche
keywords and the thumbnail hook from them rather than from the working title
alone.
